import { FetchCardPipe } from './fetch-card.pipe';

describe('FetchCardPipe', () => {
  it('create an instance', () => {
    const pipe = new FetchCardPipe();
    expect(pipe).toBeTruthy();
  });
});
